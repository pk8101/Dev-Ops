			GIT CLASS 4


git update-ref -d HEAD -->it will delete all commits

senerio:
if we have 5 files and we commit only 4 files then to commit all files in one commit then:
git commit --amend --no-edit-->it will add automatically cos it is staging


Temp delete:
git stash --> to temp delete the changes
git stash apply --> to get the data back that we deleted temp
git stash list -->to get list of stash 
git stash drop stash@{2}-->to delete particular stash data
git stash clear -->to delete all stash





